% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csfmt_rts_v3.R
\name{set_csfmt_rts_data_v3}
\alias{set_csfmt_rts_data_v3}
\alias{csfmt_rts_data_v3}
\title{Convert a data.table to csfmt_rts_data_v3 (clean csfmt; explicit healing)}
\usage{
set_csfmt_rts_data_v3(x, create_unified_columns = TRUE, heal = TRUE)

csfmt_rts_data_v3(x, create_unified_columns = TRUE, heal = TRUE)
}
\arguments{
\item{x}{The data.table to convert (by reference).}

\item{create_unified_columns}{Create the unified columns?}

\item{heal}{Derive the missing time and geography columns on creation? These are deterministically looked up from \code{isoyearweek} and \code{location_code}; nothing is statistically imputed and no count is invented.}
}
\value{
x, modified by reference, invisibly.

A new csfmt_rts_data_v3 (not by reference).
}
\description{
Eleven unified columns, taken from the 18 of
\code{\link{set_csfmt_rts_data_v2}}: granularity_geo, country_iso3,
location_code, age, sex, isoyear, isoweek, isoyearweek, season, seasonweek
and date. It drops the self-healing \code{[} override (healing is explicit) and
gives \code{time_series_id} a content hash.
}
\examples{
# v3 is weekly-only: the other time columns come from isoyearweek alone.
d <- data.table::data.table(
  isoyearweek = c("2020-34", "2020-35"),
  location_code = "nation_nor",
  deaths_n = c(1L, 2L)
)

# set_csfmt_rts_data_v3() converts d in place and returns it invisibly.
cstidy::set_csfmt_rts_data_v3(d)
d[]
class(d)

# csfmt_rts_data_v3() copies instead, so e is left as it was.
e <- data.table::data.table(
  isoyearweek = c("2020-34", "2020-35"),
  location_code = "nation_nor",
  deaths_n = c(1L, 2L)
)
y <- cstidy::csfmt_rts_data_v3(e)
class(y)
class(e)
names(e)
}
\seealso{
No vignette covers csfmt_rts_data_v3. The data-format vignette
documents the csfmt_rts_data_v2 columns that this format takes its own from:
\code{vignette("csfmt_rts_data_v2", package = "cstidy")}.

Other csfmt_rts_data:
\code{\link[=expand_time_to]{expand_time_to()}},
\code{\link[=identify_data_structure]{identify_data_structure()}},
\code{\link[=remove_class_csfmt_rts_data]{remove_class_csfmt_rts_data()}},
\code{\link[=set_csfmt_rts_data_v1]{set_csfmt_rts_data_v1()}},
\code{\link[=set_csfmt_rts_data_v2]{set_csfmt_rts_data_v2()}},
\code{\link[=unique_time_series]{unique_time_series()}}

Other csfmt format converters:
\code{\link[=set_csfmt_rts_data_v1]{set_csfmt_rts_data_v1()}},
\code{\link[=set_csfmt_rts_data_v2]{set_csfmt_rts_data_v2()}}
}
\concept{csfmt format converters}
\concept{csfmt_rts_data}
